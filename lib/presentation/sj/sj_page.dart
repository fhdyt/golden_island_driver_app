import 'package:flutter/material.dart';

class SjPage extends StatelessWidget {
  const SjPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Surat Jalan"),
      ),
    );
  }
}
